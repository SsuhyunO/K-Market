<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.k_market.dao.ReviewDAO">

    <insert id="insertReview" parameterType="org.example.k_market.dto.ReviewDTO">
        INSERT INTO review (orderItemNo, prodNo, memberUid, rating, content, fileId, createdAt)
        VALUES (#{orderItemNo}, #{prodNo}, #{memberUid}, #{rating}, #{content}, #{fileId}, NOW())
    </insert>

</mapper>
